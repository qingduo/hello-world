import matplotlib.pyplot as plt
import numpy as np
import pandas as pd
import scipy as sp
import requests as rq

x1=np.linspace(0,100,1000)
np.vstack([x1, np.ones_like(), np.ones_like()])
y1=100-x1
y2=-1*(100-x1)
x2=-1*x1
plt.plot(x1,y1)
plt.plot(x1,y2)
plt.plot(x2,y1)
plt.plot(x2,y2)
plt.show()

